name: CD Pipeline

on:
  workflow_run:
    workflows: ['Docker Image CI']
    types:
      - completed

env:
  GITHUB_HASH: $(git rev-parse --short "$GITHUB_SHA")

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - name: Access Infra
      run: cd infra